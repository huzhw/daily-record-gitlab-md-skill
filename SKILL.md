---
name: daily-record-gitlab-md
description: 记录当天需求到日报 md：从 GitLab 提交记录提取当天需求、按链路卡分型机械计价评估工时（开发类走五维单价表；运维/实施类按实测记；文档类走 docs/memory 文档单价表；AI 辅助工时＝人工工时 ÷ 压缩比 K（双轴分型），计价表 v2.0），直接写入 md 日报表，全程自动零确认；后续合并、导出 Excel、发邮件由日报管家自动执行。触发词：补充日报、写日报、记录需求、日报总结、今天干了什么。
author: 胡志伟
motto: "工时评估不是拍脑袋——设计、改动、测试、返工、沟通，五个维度逐一展开，少算一个就是对自己的不尊重。"
---

# 日报 — 记录当天需求

## 🔴 写完 md 即收工 —— 合并/发件交给日报管家

**本 skill 只做一件事：把当天需求从 GitLab 提交记录里提取出来、评估工时、写入 `日报需求记录-{日期}.md`，写完即结束。**

**🔴 全自动零确认**：合并判断、工时评估、写入 md 全程不等用户确认——**先判任务类型（开发类 / 文档类 / 运维实施类）**：开发类先「需求分型 + 查链路卡（chains.md）」再查「工时计价表」固定单价机械相乘，AI 只数数量不选值；**文档类**（`docs/` / `memory/` / `memory/ai/` 下当天新增或修改的文档，不含代码改动）按「文档类单价表」数篇数 / 行数 / 处数；**运维/实施类（无 git 改动：装库、部署、配置对齐、启停、发版、备份）不进单价表**，两列工时都记实测（见「运维/实施类记录口径」）。AI 辅助工时按 v1.9 口径＝**人工工时 ÷ 压缩比 K（轴 2 档）**（🈲 不按时间窗口 / 会话数推）；算完直接写入，写后输出「写后审计块」（开发类 / 文档类＝人工公式 + K 折算明细；运维/实施类＝实测起止与时长，同步落盘 md 末尾）供事后核对，有异议直接改 md 对应行。

- 不引导、不提醒用户再跑合并 —— **合并到当月累积 Excel、导出、归档、发邮件都由日报管家（daily-report-panel）自动完成**，md 所在目录就是日报管家的数据区，写完 md 它自己会抓。
- 本 skill 没有"第二步"，也不要主动提示"之后去合并日报/生成表格"之类的话。

**触发词只属记录环节**：说"补充日报/写日报/记录需求/日报总结/今天干了什么"就触发本 skill，不连带任何合并动作。

---

## 多项目：在哪个仓库跑就记录哪个

每天手上常有多个项目要填日报，**每个项目一条流程，各记各的 md 行**：

1. 进入该项目 git 仓库目录（或在其工作目录下调用）
2. 对该仓库执行：`today_commits.py` 汇总今日提交（内置作者过滤）→ **增量过滤（第 1.5 步）** → 合并任务 → **判任务类型（开发类 / 文档类 / 运维实施类）** → 开发类走需求分型 + 查链路卡 + 五维度评估；文档类按「文档类单价表」算篇数 / 行数 / 处数；运维/实施类不进单价表、两列记实测 → **写入当天 md 的那个仓库行**
3. 当天 md 文件：`日报需求记录-{YYYY}-{MM}-{DD}.md`，各项目共用同一个文件，**追加**（Append）该仓库行，不覆盖、不重建
4. 当天所有项目都记完后即收工 —— 合并/导出/发件由日报管家自动完成，无需再手动跑合并

> md 文件创建：当天第一次记就在报告目录里创建（按上表路径）；之后各项目只追加行，除非日期变了。
>
> 🔴 **无 commit 的运维/实施工作也要记**（服务器装库、部署、配置对齐、启停、发版）：提交id 列写 `无`，按「运维/实施类记录口径」两列记实测——不许因为「没有提交」就漏记，也不许拿代码单价表硬算。
>
> 🔴 **文档类照记**：`docs/`、`memory/`、`memory/ai/` 下当天新增或修改的文档（含 `memory/ai/` 这种被 .gitignore 排除、压根没有 commit 的）走「文档类单价表」——别当成「没提交就不记」，也别混进开发类按代码项数算。

---

## 日报文件

```
F:\idea-workspase-skills\daily-report-panel\报告-{年份}年\日报-{年份}-{MM}月\日报需求记录-{YYYY}-{MM}-{DD}.md
```

**月份一律两位补零**（如 `08月`），与文件名 `{MM}` 及日报管家目录识别规范一致（优先 `08月`，兼容历史 `8月`）。目录定位用脚本，**禁止手工推算月份数字**：

```bash
python "{技能目录}/scripts/report_dir.py"              # 今天
python "{技能目录}/scripts/report_dir.py" 2026-08-03   # 补历史日报（YYYY-MM-DD）
```

脚本输出当月报告目录绝对路径：优先匹配 `日报-{年}-{MM}月`，兼容历史遗留的 `日报-{年}-{M}月`，都不存在则创建 `日报-{年}-{MM}月`。

**脚本失败时手工兜底**（按顺序）：① 找 `日报-{年}-{MM}月` → ② 找 `日报-{年}-{M}月` → ③ 创建 `日报-{年}-{MM}月`。

当前日期从系统获取。md 文件不存在则按模板创建。

---

## 仓库列口径

日报「仓库」列**直写 git 仓库原名**（如 `ai_data_infra`），不做映射——中文项目名由日报管家第一步合并时按 `repo_map` 映射（面板「项目映射页」维护，合并时点生效、不回改历史），本 skill 不维护第二份映射，防两边漂移。

> 🔴 **「git 仓库原名」定死 = remote URL 末段去掉 `.git`**（如 `http://host/huzhiwei/workingpaper-v5.5.git` → `workingpaper-v5.5`），**不是本地目录名**——本工作区目录名常与仓库名不同：目录 `standard_thdg_zxdm` → 仓库 `workingpaper-v5.5`；目录 `fileCluster` → 仓库 `lanxum-filecuster`。写错名字会让 `recorded_commits.py` 报 `repo_not_found_in_md`、去重直接失效。

🔴 **评估前必查链路卡**：每个仓库的「数量口径 + 标准触点清单 + 已知坑」在 `{技能目录}/chains.md`，**按 git 仓库名直查**；没有对应卡的仓库用「通用兜底卡」，首次记录后按实证提案转正式卡（见「自我校准」）。

> **仓库列只写 git 仓库原名**；GitLab 网页链接 / 分支 / commit id / 提交日期进需求概述末尾的溯源括号（见第 6 步），不在仓库列混写。

---

## 表格格式

| 序号 | 日期 | 仓库 | 需求概述 | 涉及模块 | 状态 | 人工工时(h) | AI辅助工时(h) | 备注 | 提交id | 实测用时 | 任务分型 |
|------|------|------|---------|---------|------|-------------|---------------|------|--------|---------|---------|

- **序号**：自动递增，解析现有表最后一个序号 +1
- **日期**：格式 MM-DD
- **仓库**：git 仓库原名直写，不做映射（中文项目名由日报管家合并时按 repo_map 映射，见「仓库列口径」）
- **需求概述**：🔴 写详细，**用 dash 多级编号**（`1、` → `1-1` → `1-1-1`，见「第 6 步」写法示例）列具体改动（类名、技术决策、踩坑点、文件规模），让读者仅看概述就能判断需求难度，禁止一句话带过。**每个编号行之间用 `<br>` 换行**（markdown 表格单元格内不能直接换行，`<br>` 是标准写法；日报管家会把 `<br>` 转成 Excel 单元格换行、按 dash 深度直出层级）。**最后一行溯源括号必带 GitLab 网页链接 + 分支 + commit id + 提交日期**（格式见第 6 步）
- **涉及模块**：改动涉及的模块/包路径简述
- **状态**：百分比 — `100%`（已完成）、`0%`（未开始）、`50%`（进行中）
- **任务类型**：🔴 v1.6/v1.7，评估前先判三类——**开发类**＝产出代码/配置（走五维单价表）；**文档类**＝`docs/`、`memory/`、`memory/ai/` 下当天新增或修改的文档且无代码改动（走「文档类单价表」，AI 列记实测）；**运维/实施类**＝无 git 改动的服务器/环境操作（两列都记实测）。🔴 判据冲突取高：同一需求含代码文件改动即算开发类（文档并入，不重复计）
- **人工工时(h)**：🔴 分任务类型（v1.6/v1.7）——**开发类**＝手写等效时间，不是日历耗时，AI 辅助下可远超 24h（详见第 4 步工时关系说明）；**文档类**＝「文档类单价表」机械求和（等效产出）；**运维/实施类**＝实测耗时（人实际干了多久）
- **AI辅助工时(h)**：🔴 v1.8 口径＝**人工工时 ÷ 分型压缩比 K**（K 由「轴 2 压缩比档」定死：① 机械型 8.0 / ② 参照型 4.0 / ③ 常规型 3.0 / ④ 跨模块型 2.5 / ⑤ 深挖型 2.0 / ⑥ 无参照型 1.5）——把人工等效产出折算成人机配合实际耗时，**不是**掐表实测、也不用会话数反推；该列即日报管家读的「预估AI辅助工时」，压缩比恒等于 K；算法见「AI 辅助工时」节
- **备注**：关键数字、修复的 bug、特殊说明。🔴 **数量口径定死**：文件数 = 本条需求 diff 内**源码文件去重后**个数；行数 = **源码 insertions 求和**（写成 `+X/-Y 行`）；vite/webpack 构建产物、`min` 文件、`*.tsbuildinfo`、二进制、纯重命名迁移**一律不计**（只能进备注文字，不进数字）；**文档类**写文档文件数 / 新增行数（如「docs 2 篇 +180 行」）；**运维/实施类**（无 commit）不写文件数/行数，写对象数 + 实测耗时。🔴 **末段必带该行耗时窗口** `⏱ MM-DD HH:MM~HH:MM（约 Xh）`（运维/实施类填**实测起止**，🔴 禁止借用其他行的提交时间窗）
- **提交id**：🔴 **第 10 列，必填**——本条需求全部 commit 短 id，`/` 分隔；跨分支标分支名 `1a2b3c(master)/4d5e6f(feature-1)`；无提交（纯数据运维等）填 `无`。增量去重脚本按这列机械判重（按位置读第 10 列），漏填会导致下次误判重复记录。日报管家解析只取前 9 列，此列不进库、不影响管家
- **实测用时**：🔴 **第 11 列，2026-09-12 架构师指示新增**——该行实际墙钟耗时（写 `约 Xh`，与备注 `⏱` 同源取值）；一天多需求切来切去、窗口碎掉没有可信实测时填 `—`。**纯记录列**：不参与人工/AI 任何计价与推导，判重脚本按位置读第 10 列不受影响、日报管家不解析——先攒数据，用法（如喂 K 校准）后议
- **任务分型**：🔴 **第 12 列（末列，2026-09-12 增，替代原"备注首段标注"方案）**——把分型结论写成表格直接可读的完整文字，让读者不看审计注释就懂这行怎么算的。取值格式（类与档必须带全称与含义缩写）：
  - 开发类：`开发类（C 新功能需求）· 档④ 跨模块型 · K=2.5`
  - 文档类：`文档类（F 文档）· 档③ 常规型 · K=3.0`
  - 运维/实施类：`运维类（E 运维实施）· 无 K（两列实测）`
  - **六类含义**（轴 1）：A=文案/配置、B=缺陷修复、C=新功能/需求、D=重构/迁移、E=运维/实施、F=文档
  - **六档含义**（轴 2）：①=机械型 K8.0、②=参照型 K4.0、③=常规型 K3.0、④=跨模块型 K2.5、⑤=深挖型 K2.0、⑥=无参照型 K1.5（判据详见「分型双轴」）
  - **纯展示列**：与末尾审计注释块同源、不参与计价推导；判重脚本按位置读第 10 列不受影响、日报管家不解析
  - 🔴 **每次写完表格必须在表格下方追加图例段**（blockquote，供读者不看 skill 就懂这列怎么算）——固定文字模板：`> 📖 **任务分型列说明**：所有任务先分六类（A 文案/配置、B 缺陷修复、C 新功能/需求、D 重构/迁移、E 运维/实施、F 文档），决定"人工工时"按什么规则数——本行为 <C 类具体写法>。再按 AI 提速难度分六档（① 机械型 K=8.0、② 参照型 K=4.0、③ 常规型 K=3.0、④ 跨模块型 K=2.5、⑤ 深挖型 K=2.0、⑥ 无参照型 K=1.5），<该档触发条件一句话>——本行为 <档④等具体写法>。K=AI 压缩比：AI辅助工时 = 人工工时 ÷ K，本行 <人工> ÷ <K> = <AI>，即"手写需 <人工> 小时的活，人机配合预估 <AI> 小时"。`（位置：表格结束后空一行，审计注释块之前；管家/判重脚本均不解析非表格行）

---

## 流程

### 第 1 步：汇总当日全部提交 🔴 一个脚本出全量，禁止手工拼命令

🔴 **只统计自己的提交**：`today_commits.py` 内置 `--author="胡志伟"` 过滤（作者身份以 `git log --format="%an <%ae>"` 输出的 `胡志伟 <huzhiwei@lanxum.com>` 为准），同事提交一律不进结果。在目标仓库目录下运行：

```bash
python "{技能目录}/scripts/today_commits.py"              # 今天
python "{技能目录}/scripts/today_commits.py" 2026-09-05   # 补历史（YYYY-MM-DD）
```

一次输出 JSON（提取字段用 `jq`，禁止 `python -c` 二次处理）：全部提交（短 id / 时间 / 主题，按时间升序）、涉及文件数、+/- 行数（**逐 commit numstat 求和**，二进制文件只计文件不计行）、时间窗、分支、origin 与规整后的 GitLab 网页链接。**列出全部提交，禁止 `-n` 截断。**

- `no_commits=true` → 直接回复「该仓库今日无可记录提交」收工——不创建、不修改 md，不凭会议记录或记忆编造需求行
- `web_url_resolved=false` → 溯源括号里整段贴 origin 原文，不猜、不硬编码 host
- **提交日期口径**：汇总/判重用完整 `YYYY-MM-DD`（JSON 的 `date` 字段），合并展示与 md 写入用 `MM-DD`（与表格日期列一致）

汇总展示格式（数值全部取自 JSON，不另跑命令）：
```
今日共 N 次提交（2026-09-05，13:42~13:52），涉及 M 个文件，+XXX/-XXX 行
仓库链接：https://gitlab.xx.com/g/work-v5.5（分支 master）
commit：1a2b3c/4d5e6f（09-05）
```

🔴 **脚本失败时才手工兜底**（同样只读自己的提交）：
```bash
git log --author="胡志伟" --since="YYYY-MM-DD 00:00:00" --until="YYYY-MM-DD 23:59:59" --format="%h %ai %s" --numstat
git remote get-url origin
git branch --show-current
```
> 🔴 **禁止用 `git diff --stat A..B` 统计变更量**——`git diff` 不支持 `--author` 且**静默忽略不报错**（2026-09-09 实测），同事提交会混进总量；行数一律以 numstat 逐条累计为准（二进制行 `-` 跳过）。commit id 用短 id（`%h`，如 `1a2b3c`），GitLab 支持短 sha 直接跳 `/-/commit/<短id>`。

### 第 1.5 步：增量过滤 🔴 机械比对，禁止 AI 肉眼比对 md

同一天多次「补充日报」（多仓库各记各的、或同仓库补记）时，**只处理还没写进当天 md 的 commit**。第 1 步拿到短 id 后立刻跑脚本，把判断交给机器：

```bash
python "{技能目录}/scripts/recorded_commits.py" <仓库列中文名> <短id> <短id> ...        # 今天
python "{技能目录}/scripts/recorded_commits.py" 2026-09-06 <仓库列中文名> <短id> ...   # 补历史
```

- 参数：`[日期]`（缺省今天）+ `[仓库名]`（= md 仓库列的值，🔴 传 id 时必传，漏传脚本直接报错——防止跨仓库 id 撞车造成漏需求）+ 第 1 步拿到的**全部**短 id
- 🔴 **补记过渡**（仓库列改原名直写后）：同一天某仓库已用旧版中文名记录过，补记传的仓库名**照抄 md 已有写法**——同名异写（如 中信底稿V5 / workingpaper-v5.5）会让判重差集误判漏记
- 脚本解析当天 md 已记录的「仓库 + 提交id」二元组（含日报管家 `已合并\` 里当天的分片），机械算差集，输出 JSON；提取字段用 `jq`，禁止 `python -c` 二次处理
- `all_recorded=true` → 回复「该仓库今日提交已全部记录过」，收工，不重复记
- `unrecorded` 非空 → **只有这些 commit** 进第 2 步合并评估，已记录的不重复算工时
- `rows_without_ids` 非空 → 有行拿不到提交id（历史 9 列格式 / 忘填 / 解析失败），去重对那批 commit 失效，**向用户说明并人工核对后再决定是否记录**
- `repo_not_found_in_md=true` → 传的仓库名与 md 任何行都不匹配，先核对 md 里该仓库的写法（旧版中文名 / 原名）再继续，防止全判"未记录"造成重复
- md 不存在（当天还没记过）→ 全部视为未记录，走原流程

🔴 **防漏两条（宁重复、勿漏记）**：

1. **合并展示时必须列出「已跳过（已记录）」的 commit 清单**——被去重过滤掉的 commit 逐个列出，随合并结果一起给用户过目；用户发现"某个做过的需求不在清单也不在合并结果里"就是漏了，立刻核查
2. **写入后自检闭环**——md 写完后复跑一次脚本，传入本次记录的全部短 id：`unrecorded` 必须为空；非空 = 新行的提交id 列漏写/写错，当场修复再收工

### 第 2 步：合并相似任务 🔴 核心，不准跳过

一条日报记录 ≠ 一次 git commit。**必须**把以下情况合并为一条：

| 合并条件 | 判断方法 |
|---------|---------|
| **同文件反复改** | 多个 commit 改同一个文件 → 合并 |
| **同模块同类改动** | commit 涉及同一个包/模块，且改动性质相似（如都是优化、都是修 bug）→ 合并 |
| **同一功能全链路** | 前端+后端+XML+构建产物，只要属于同一个功能需求 → 合并 |

**合并策略**：

1. **先按模块/功能聚拢**：把所有 commit 按改动领域分组
2. **能合则合**：如果多个分组都属于同一个大方向（如"v3 页面建设""目录日志迁移"），合并为一个顶层需求，内部用 1、2、3…… 编号分述子项
3. **优先单行**：同一天同一个仓库，优先合并成**一条**大需求，子项用编号区分；只有改动分属**互不相关的独立任务**（业务方向不同、无共同链路）时才拆成多行——拆行是例外不是默认
4. **上表只管「顶层合并」**：合并条件表与本策略只决定**几个 commit 合成一行**；行内**子项怎么切**另按下方 v1.5 簇规则。同一 commit 同时交付多端（如后端 + React 页）时，按目录/模块**照拆多个簇**——不因「同一个 commit」就不拆。两处判据不冲突：顶层看「是不是一个需求」，子项看「是不是一条链路簇」

**合并输出格式**（合并结果进「写后审计块」展示，不再前置等确认；每个子项附 commit id、分支与提交日期）：

```
## 合并结果

### 需求：[一句话概括今天做了什么]

#### 1. [子项名称]
- N 次提交（09-05 13:42~13:52 · 分支 master）
- 涉及文件：X 个
- 变更量：+XXX/-XXX 行
- commit：1a2b3c/4d5e6f（09-05）

#### 2. [子项名称]
- N 次提交（09-05 14:10~14:30 · 分支 feature-1）
- 涉及文件：X 个
- 变更量：+XXX/-XXX 行
- commit：7f8a9b（09-05）

#### 3. ...
```

跨分支（同一需求涉及多个分支）时，commit 逐个标分支：`1a2b3c(master)/4d5e6f(feature-1)`。

🔴 **合并完不问用户**——机械规则合完直接进工时评估（第 4 步），合并结果随「写后审计块」给用户过目。

> 🔴 **子项切分与折扣（v1.5 机械规则，消灭「同一份数据两次跑不一致」）**
>
> **① 切子项**：同一顶层需求内，把改动按**业务链路簇**聚拢——同一模块 / 同一目录前缀、或互相引用的改动归一个子项，**同簇的提交一律合并、不许按目录再切碎**；零交集的簇各自成子项。
> **② 定锚**：先剔除**依附型簇**（见 ④），剩下的簇里取**源码 insertions 最大**的为「主链路子项」（比较时同样不计构建产物）；**若剩不下任何非依附簇**（整行全是依附型），则整行视为**单一子项、不打折**（简单修复类常态，见 2026-09-10 行3）。
> **③ 折扣**：主链路子项**不打折**；其余每个子项（含全部依附型簇）**×0.6**，输出标注 `（×0.6）`。
> **④ 依附型簇（机械判据，命中任一即是）**：a) 纯注释 / 文档 sweep——新增行 >80% 以注释符（`//` `/*` `*` `#` `<!--`）开头或为空白；b) 配置 / 文案 / 菜单名 / 版本号类——无新增源码文件 且 源码 insertions ≤ 20；c) 单点收尾修复——该簇内仅 1 处差异修改。
> **⑤ 实例**（2026-09-10 workingpaper-v5.5）：簇 = 采集链路后端（≈719 行，锚，不打折）/ React 查询页（≈547）/ 统计页（≈390）/ Quartz 注释 sweep（1290 行，依附 a）/ 菜单更名（1 行，依附 b）→ 后四者 ×0.6。注：后端与查询页同出 `139384582` 一个 commit，按「同一 commit 多端交付照拆」判据仍分两簇。

---

### 第 3 步：打开日报文件

先运行目录定位脚本拿到当月目录（见上文「日报文件」），再根据当前日期在对应目录下找到 `.md` 文件。不存在则按模板创建：

```markdown
# 日报需求记录 — YYYY-MM-DD

**日期：** YYYY-MM-DD
**作者：** 胡志伟

---

| 序号 | 日期 | 仓库 | 需求概述 | 涉及模块 | 状态 | 人工工时(h) | AI辅助工时(h) | 备注 | 提交id | 实测用时 | 任务分型 |
|------|------|------|---------|---------|------|-------------|---------------|------|--------|---------|---------|
| 1 | | | | | | | | | |
| 2 | | | | | | | | | |
| 3 | | | | | | | | | |

> 空行为模板，新需求逐行追加。
```

**检查文件头日期**：如果文件标题日期与文件名日期不一致，先修正。

**仓库名**：git 仓库原名直写（见「仓库列口径」），不做映射。

---

## 🔴 Windows bash 执行规范（反复踩坑，禁止违反）

- **禁用 `python -c "多行代码"`**：本环境 bash 带 cmd 包装层，内联代码含 `||`/`&&`/换行会被 `goto :error` 拦截，报 `IndentationError: unexpected indent`。要多行处理 → 用 Write 写临时 `.py` 文件再 `python 文件.py`。
- **JSON 用 jq**：`命令 | jq '.字段'` 提取，**禁止** `python -c` 二次处理。git log / officecli 输出都适用。
- 🔴 **`python` 被拒时改用解释器绝对路径**：本机 `python.bat`（pyenv-win shim）在部分 Agent 环境报 `Access is denied`、且无 `py` 命令。兜底顺序：① `pyenv which python` 取绝对路径；② 直接调 `"$env:USERPROFILE\.pyenv\pyenv-win\versions\3.11.9\python.exe" 脚本.py`（版本用 `pyenv versions` 查）。脚本无第三方依赖，任意 Python 3.7+ 均可。

---

### 第 4 步：工时评估（按合并后的任务逐条评估）

🔴 **全自动，不问不确认**：**先判任务类型（开发类 / 文档类 / 运维实施类）**——**开发类**先定需求分型、查 chains.md 链路卡，再按「工时计价表」逐维度**数出数量 × 固定单价**；**文档类**按「文档类单价表」数新建篇数 / 新增行数 / 修改处数；**运维/实施类**（无 git 改动）不进单价表，按「运维/实施类记录口径」两列记实测。AI 辅助工时按 v1.9 口径（**人工工时 ÷ 压缩比 K（轴 2 档）**）算，🈲 不按单价表求和、不按时间窗口推。算完直接进第 5 步写入。**不准给区间值**（单价已定死），**不准跳过维度直接报总数**（开发类 / 文档类），**不准跳过分型/链路卡**。

---

## 工时评估规则

> **计价表版本 v2.0（2026-09-11）**——本节及「AI 辅助工时」全部固定单价同属本版本；调整任一单价或取整/分型/链路卡口径必须升版本号并记日期，历史日报按当时版本可复算，**禁止静默改价**（调价走「自我校准」提案制，架构师确认后生效，双向可调）。
>
> 版本演进明细（v1.0 ~ v1.9）见本仓 git 历史。现行口径要点：双轴分型（轴 1 类 A~F 定人工数法 / 轴 2 档①~⑥ 定压缩比 K）、AI 列＝人工 ÷ K、D 类新增项按 `min(实际项数, 源单元数)` 封顶；已废口径（墙钟式、AI 单价求和式）一律不复算。

### 任务类型 🔴 v1.6/v1.7 · 先判类型再分型（安装软件、写文档 ≠ 写代码）

| 任务类型 | 机械判据 | 计价方式 |
|---------|---------|---------|
| **开发类** | 产出代码 / 配置（.java/.tsx/.jsp/.sql/.yml/pom 等），改动进 git | 走下方五维单价表 + chains.md 代码链路卡 |
| **文档类** | 产出是文档：项目仓库根目录 `docs/` / `memory/` / `memory/ai/`（含子目录）下当天新增或修改的文档，**不含代码改动** | 走「文档类单价表」（人工列＝等效产出）+ AI 列记实测 |
| **运维/实施类** | **无 git 改动**，是服务器 / 环境操作（装库建库、软件安装部署与升级、参数配置、服务启停、发版、备份、数据脚本执行与核对、连通冒烟） | **不进单价表**，按「运维/实施类记录口径」两列记实测 |

> 🔴 **判据冲突取高**：同一需求只要含代码文件改动，就算**开发类**（文档并入、不重复计）；纯文档需求（含只改文档的 commit）单列**文档类**；无任何 git 改动的服务器 / 环境操作才是**运维/实施类**。

> 🔴 安装软件、部署环境、写文档**都不是**写代码：**运维/实施类**禁止套用五维单价表（设计调研 / 代码改动 / 测试验证 / 风险返工）和 chains.md 的文件数、行数、接口回归单价（分型写「运维」、卡查「服务器运维/实施卡（卡 7）」）；**文档类**走「文档类单价表」（分型写「文档」、卡查「文档类卡（卡 8）」）。两类都不适用 `AI ≤ 人工` 护栏。

### 分型双轴 🔴 评估前机械对号（v1.9）

开发类任务**两轴独立取号**，两个值都写进审计块：**轴 1 = 类**（决定人工工时怎么数）、**轴 2 = 档**（决定压缩比 K）。两轴**不对齐**——存在「人工高 + K 高」（批量模板生成）与「人工高 + K 低」（疑难排查）两类反例，一根轴表达不了。运维/实施类写「类=E 运维」、文档类写「类=F 文档」（两者都不取档、不取 K）。

#### 轴 1｜人工估时分类（决定人工工时怎么数）

| 类 | 机械判据 | 人工工时怎么数 |
|----|---------|---------------|
| A 文案/配置 | 无逻辑的文案、常量、配置、版本号 | 只数「机械替换 + 入口回归」 |
| B 缺陷修复 | 改现有逻辑，不动接口契约 | 数差异修改处 + **根因层数** |
| C 新功能/需求 | 新增接口 / 页面 / 表 / 链路 | 数新增项 + 上下文切换 |
| D 重构/迁移 | 外部行为不变、内部换实现 | 数迁移项 + 回归面（新增项封顶见「维度 2 · D 类新增项封顶」） |
| E 运维/实施 | 无 git 改动 | 不进五维，记实测 |
| F 文档 | `docs/` / `memory/` / `memory/ai/` | 走文档类单价表 |

#### 轴 2｜压缩比档（决定 K）

| 档 | 机械判据（AI 只对号不选值；冲突取更高档） | K |
|----|-------------------------------------------|---|
| ① 机械型 | 模板、批量替换、纯文案，无需逐处判断 | 8.0 |
| ② 参照型 | 有同类成熟参照，照结构抄 | 4.0 |
| ③ 常规型 | 常规逻辑，有参照但需动脑 | 3.0 |
| ④ 跨模块型 | 跨 ≥3 个**业务模块/子系统**（如：采集+归档+流程三域联动。🔴 前后端分层与配套配置是任何功能的标配形态，不计入，否则所有全栈需求都成④），但有参照 | 2.5 |
| ⑤ 深挖型 | 需读第三方库/框架源码；或根因链 ≥2 层；或无参照的排查类 | 2.0 |
| ⑥ 无参照型 | 全新技术引入、无市面直接参照 | 1.5 |

- 两轴是**数法依据 + K 取值依据**，不改五维单价；K 是旋钮（制度参数），判据是机械的，调 K 走「自我校准」提案制
- 数数量口径按 chains.md 对应链路卡：新增代码「一项」在 Java=一个类/一组 Mapper+XML、React=一个组件三件套、JSP=一个页面块、Python=一个函数/处理段；差异修改「一处」= 一个需要单独判断的改动点

### 维度 1：设计调研

| 子项 | 评估逻辑 | 固定单价 |
|------|---------|------|
| 现状摸排 | 需要读几个文件？梳理几种现有模式/写法？ | 每类模式 0.3h |
| 方案设计 | 需要设计新 API、新类、新方法？几个重载？有无异步/并发/多线程场景要专门设计？每项分开算 | 每项 0.8h |
| 依赖追溯 | 要不要追继承链、框架默认行为、配置覆盖？ | 每项 0.4h |
| 方案对齐 | 需要出方案等用户确认 | 每项 0.8h |

---

### 维度 2：代码改动

| 子项 | 评估逻辑 | 固定单价 |
|------|---------|------|
| 新增代码 | 新类/新方法/新接口？按项数计 | 每项 0.7h |
| 机械替换 | 完全相同的模式逐文件替换（IDE 搜索替换可批量完成） | 3min/处 |
| 差异修改 | 每处需要单独判断——用哪个重载、参数来源、有无特殊逻辑、是否异步 | 8min/处 |
| 上下文切换 | 跨模块越多，加载理解新文件上下文的时间越长 | 每 5 个模块 +0.3h |

**禁止把差异修改当机械替换估**——只要每处需要人工决策，就是差异修改。

**同文件反复改**：同一轮会话中反复改同一个文件（修字段名、补参数、改编码），上下文已加载：
- 首次修改：正常按新增/差异评估
- 后续修改：按机械替换标准（3min/处），不计上下文切换

**构建产物 / 纯机械迁移不计项数**（v1.2）：vite/webpack 构建产物、min 文件、包重命名迁移、批量正则替换产物——只进备注标注数量，**不计**新增代码项数、不计上下文切换，防 diff 文件数虚增估值。

**D 类新增项封顶（v2.0，防风格膨胀）**：重构/迁移的「新增代码」项数以**迁移源单元数为上限**——源单元数 = `git diff` 前一版里被迁移代码所在同包/同目录的单元数（口径同「一项」：Java=一个类/一组 Mapper+XML、React=一个组件三件套）。迁移中拆类、抽方法、重组目录产生的**多余**单元并入已计迁移项、不另计新增项，即新增项按 `min(实际项数, 源单元数)` 计；迁移中**外部行为变化**的部分（新接口、新字段、新交互）不受封顶，按 C 类新增正常计价。审计块须附封顶算式（`源单元数 N → 新增项 min(实际, N)`）。

---

### 维度 3：测试验证

| 子项 | 评估逻辑 | 单耗 |
|------|---------|------|
| 操作入口 | 列出每个受影响的用户操作按钮/入口，逐个正常流程回归 | 5min/个 |
| 异步场景 | 定时器、邮件、批量任务需要构造触发条件，还要查日志确认 | 12min/个 |
| 关键异常 | 挑关键异常分支、边界条件走一遍 | 5min/个 |
| 日志确认 | 每类操作后查数据库/日志表验证字段正确 | 3min/类 |

**规则**：
- **先枚举操作入口清单**，数清楚再算
- 异步场景 ≠ 看到 ThreadPool 就算——要确认能不能手动触发出日志，不能的话要构造数据

---

### 维度 4：风险返工 🔴 已完成任务用简化公式

| 任务状态 | 公式 | 原因 |
|---------|------|------|
| **100%（已完成）** | `风险返工 = 边界遗漏预估`（只加不减，不乘系数） | 修复 commit 本身就是实际返工，已计入代码改动，不再乘法预估 |
| **50%（进行中）** | `风险返工 =（设计调研 + 代码改动 + 测试验证）× 广度系数 × 深度系数 + 边界遗漏` | 未完成，返工不确定性大，保留乘法 |

**已完成任务的边界遗漏**：仅考虑未覆盖的边界（null、类型转换、兼容性等），不做乘法放大；**每个未覆盖边界固定 0.2h**。

**广度系数 / 深度系数**（仅进行中任务使用）：

| 改动广度系数 | |
|------|---------|
| < 5 文件 ×0.2，5~15 文件 ×0.3，15~40 文件 ×0.4，> 40 文件 ×0.5 |

| 影响深度系数 | |
|------|---------|
| 基础设施（全项目依赖）×1.5，核心业务 ×1.3，普通业务 ×1.0，边缘代码 ×0.5 |

---

### 维度 5：沟通协作 🔴 按天算，不按任务算

沟通协作是**整天的共享开销**，不逐任务累加。在最后一条任务评估完后，单独记一笔：

```
全天沟通协作：固定 1h/天
```

> 不再分项估算；当日会议/对齐异常多，写完 md 后直接改那一行数字。

---

## 人工工时计算公式

```
人工工时 = Σ(每条任务的设计调研 + 代码改动 + 测试验证 + 风险返工) + 全天沟通协作
```

取整规则（v1.3）：五维度求和后**四舍五入到 0.5h**——×2 → round → ÷2，正中间（.25/.75）向上：
- 例：0.6h → 0.5h、2.3h → 2.5h、2.8h → 3h、9.4h → 9.5h
- 无最低 1h 档、无 >8h 向上靠档——算多少记多少，如实
- **单条不设 8h 上限、不再拆分**——人工工时是等效产出，超 8h 正常（见「AI 辅助下的工时关系」，v1.1 起）
- 🔴 **运维/实施类不走本式**（无代码项、无维度可数，见「运维/实施类记录口径」）

---

## 运维/实施类记录口径 🔴 v1.6 新增（安装软件 ≠ 写代码）

**判据**：当天该需求**没有 commit**（改动不进 git）——服务器装库/建库、软件安装部署与升级、环境与参数配置、服务启停、发版、备份、数据脚本执行与对象核对、连通冒烟。

| 列 | 记什么 | 落盘写法 |
|----|--------|---------|
| 人工工时 | **实测耗时**——人实际干了多久（用户口径），按 0.5h 粒度 | 备注末段 `⏱ MM-DD HH:MM~HH:MM（约 Xh）`＝**实测起止** |
| AI 辅助工时 | **人 + AI 配合的实测耗时**——AI 陪跑全程的实际时长（AI 提速有限时与人工列相等） | 审计块 `AI=实测 X.Xh` |

- 🔴 **分型写「运维」**，链路卡查 chains.md「服务器运维/实施卡（卡 7）」
- 🔴 **禁止借用其他行的提交时间窗**——无 commit 就没有 commit 窗口；`⏱` 必须实测起止，记不全时按「当天该工作所在时段 + 实测总时长」如实写
- 🔴 **禁止套用五维单价表与代码数量口径**（设计调研 0.3/0.8、代码改动 0.7、异步测试 12min/个、入口 5min/个、日志 3min/类、文件数/行数）——装库、部署不产生代码项，硬套就是编数（2026-09-10 序号 5 的教训）
- 🔴 **不适用 `AI ≤ 人工` 护栏**：运维/实施类两列都是实测，相等即正常
- 备注数量口径：不写文件数 / 加删行数（无 diff），写**对象数 + 实测耗时**（如「装 OB 单机 + 建库脚本对象核对 + Redis，实测 Xh」）
- 提交id 列写 `无`

---

## 文档类单价表 🔴 v1.7 新增（docs / memory / memory/ai 下的文档）

**判据**：当天产出落在项目仓库根目录 **`docs/`、`memory/`、`memory/ai/`**（含子目录）的文档（方案、SOP、规范、交接资料、说明、索引、清单），**不含代码文件改动**。同一需求含代码 → 归开发类（文档并入，不重复计）。

**数量口径（机械数，AI 只数数量不选值）**

- **文件数** = 三个目录下当天**新增 + 修改**的文档**去重**数（新增 / 修改分开数）
- **行数** = **新增行数**：有 commit 的取 `today_commits.py` numstat 的 insertions；`memory/ai/`（被 `.gitignore` 排除、无 commit）取文件实际行数
- **处数** = 改已有文档时「需要单独判断的一处改动」逐处数（结论变更、章节增删、口径修正）

| 子项 | 单位 | 单价 |
|------|------|------|
| 新建文档基础 | 每篇 | 0.5h（搭结构 + 落盘 + 索引挂链） |
| 正文撰写 | 每 100 行新增 | 0.3h（不足 100 行按 0.3h 记） |
| 修改已有文档 | 差异修改 | 8min/处（沿用代码差异修改价） |
| 机械替换 | 机械替换 | 3min/处 |
| 事实核对 / 渲染校验 | 每篇 | 5min |
| 边界遗漏 | 每处 | 0.2h |

- 取整同现规则（四舍五入到 0.5h），单条最低记 **0.25h**
- AI 辅助工时**不动**：仍 = 人工工时 ÷ 压缩比 K（档见「分型双轴」）；**不适用** `AI ≤ 人工` 护栏
- 审计块字段同开发类：`人工公式=<Xh> · AI实测=<X.Xh>`，分型写「文档」、卡写「文档类（卡 8）」
- 例算（校准）：新建 200 行文档 = 0.5 + 0.6 + 0.08 + 0.2 = 1.38 → **1.5h**；`MEMORY.md` 加一行索引 = 3min → **0.25h**；改 5 处结论 = 5×8min + 校验 0.08 + 0.2 = 0.95 → **1h**

---

## AI 辅助工时 🔴 口径 = 人工工时 ÷ 压缩比 K（计价表 v2.0）

**AI 辅助工时(h) = 人工工时 ÷ 压缩比 K**——K 由「轴 2 压缩比档」定死（见「分型双轴」），把「一个人手写要多久」折算成「人机配合实际要多久」。

> 这一列就是日报管家 / Excel 里的「预估AI辅助工时」——`TodayPanel` 积压口径与 `estimator.js`（`est = N × median(k/n)`）都读它，用途是**排期**。它是**由人工列机械推导的分型系数值**（不是掐表、不是观测值），同分型同人工必得同值，可复算、可校准。当日没用 AI 的任务如实填 0。

### 修正对话计数口径（审计块「返工<N>」用）

v1.8 前的「AI 单价求和式」（设计协助/代码生成/审查验证/修正对话四项单价）已整体废除、仅存档于 git 历史（v1.4/v1.5），禁止回用。**修正对话只数代码返工轮**：✅ 方案被驳回重做、生成代码推倒重写、跑不通返工；❌ 排查定位、复现验证、发版部署、日志核对、工时讨论、纯问答——该计数现仅作审计块证据行，不参与计价。

🔴 **三条规则（压缩比档口径）**：

1. **分母是压缩比档常数，不是观测值**——压缩比 K 由「轴 2 档」定死：**① 机械型 8.0 / ② 参照型 4.0 / ③ 常规型 3.0 / ④ 跨模块型 2.5 / ⑤ 深挖型 2.0 / ⑥ 无参照型 1.5**；`AI 辅助工时 = 人工工时 ÷ K`。同分型同人工必得同 AI，换人、换天、换窗口都不变。
2. **🈲 禁止按时间窗口 / 会话时长 / 对话轮数估**——起止窗口是人为锚点（往前挪一天、往后挪一小时结果就变），v1.6/v1.7 的「实测墙钟」口径已废；会话数、轮数只进审计块证据行。
3. **取整与人工工时同式**（四舍五入到 0.5h）；`AI ≤ 人工` 护栏天然满足（K ≥ 2），不再单列。

> 运维/实施类（无 commit）：两列都是实测、相等即正常，**不适用 `AI ≤ 人工` 护栏**。

审计块必须附**证据行**：当日 AI 会话数 / 大致对话轮数 / 修正返工轮数——**仅作自我校准与人工复核参考，不参与计价**。

---

## 🔴 AI 辅助下的工时关系

- **开发类 / 文档类**：人工工时＝等效产出（不是日历时间）；AI 辅助工时＝人工 ÷ K——AI 辅助下一天产出超 8h 正常，**不设上限、不为凑数压降或拆分**
- **运维/实施类**：两列都＝实测耗时（相等即正常，压缩比 ≈ 1.0）
- 必须标注实际工作时段：备注末段 `⏱ MM-DD HH:MM~HH:MM（约 Xh）`——人机配合起止，🔴 不取 commit 时间窗
- 压缩比＝人工 ÷ AI辅助＝轴 2 档常数 K，可复算、与时间窗口无关；🈲 不得用时间窗口/会话数/轮数推导

---

### 第 5 步：直接写入 → 写后审计块

🔴 **不等确认，算完直接写入文件**（第 6 步流程照走），写完在回复里输出「**写后审计块**」：

1. 「已跳过（已记录）」commit 清单（防漏，第 1.5 步）
2. 合并后的任务清单
3. 每条任务的算式：**开发类**＝五维度（**单价 × 数量**逐项列）+ 小计；**文档类**＝文档单价表（新建篇数 / 新增行数 / 修改处数 × 单价）+ 小计；**运维/实施类**＝实测起止 + 实测时长（不查单价表）
4. 全天沟通协作（固定 1h/天，记在当天开发类行；当天只有运维/实施类时按实测记并写明）
5. 人工工时汇总（开发类 / 文档类＝等效产出，单条可超 8h；运维/实施类＝实测耗时）
6. AI 辅助工时（v1.9）：= 人工工时 ÷ K，写清算式、轴 2 档号与 K 取值；🈲 不再按时间窗口 / 会话数推。附**证据行**（AI 会话数 / 大致轮数 / 修正返工轮数，仅参考）；运维/实施类与人工列相等即正常
7. 压缩比 = 人工工时 ÷ AI辅助工时 = **轴 2 档的 K**（① 8.0 / ② 4.0 / ③ 3.0 / ④ 2.5 / ⑤ 2.0 / ⑥ 1.5；运维/实施类两列实测相等，恒为 1.0）——压缩比不再由时间窗口推导，K 即该列除数
8. 🔴 **审计块同步落盘**：每条需求行各追加一行 HTML 注释到当天 md 末尾，字段定死机械可解析：**开发类**写 `<!-- 审计 v1.8 · seq=<行序号> · 仓库=<仓库列原值> · 类=<A 文案配置/B 缺陷修复/C 新功能/D 重构迁移> · 档=<① 机械型/② 参照型/③ 常规型/④ 跨模块型/⑤ 深挖型/⑥ 无参照型> · K=<K> · 卡=<卡名> · 人工公式=<Xh> · AI=人工÷K(<K>)=<X.Xh> · 证据=会话<N>/轮<N>/返工<N> -->`；**文档类**写 `<!-- 审计 v1.8 · seq=<行序号> · 仓库=<仓库列原值> · 分型=文档 · 卡=文档类（卡 8） · 人工公式=<Xh> · AI=人工÷K(<K>)=<X.Xh> · 证据=会话<N>/轮<N>/返工<N> -->`；**运维/实施类**写 `<!-- 审计 v1.8 · seq=<行序号> · 仓库=<仓库列原值> · 分型=运维 · 卡=服务器运维/实施（卡 7） · 人工=实测<Xh> · AI=实测<X.Xh> · 证据=会话<N>/轮<N>/返工<N> -->`；已机械验证无干扰——`recorded_commits.py` 表格后遇非表格行即 break、日报管家 mdParser 跳过非 `|` 行，注释块两边均不解析；供「自我校准」机械比对

**不设事前确认卡点**——用户看算式有异议，直接改 md 对应行的数字；改过的数字会被下次「自我校准」回看捕获为偏差信号。

---

### 第 6 步：写入表格

找到第一个空行（各列都是 `| | | | | | | | | | |` 或序号大于现有数据行）插入新行。

**仓库列**填 git 仓库原名（中文项目名由日报管家合并时映射）。

**提交id 列（第 10 列，必填）**：本条需求的全部 commit 短 id，`/` 分隔；跨分支标 `1a2b3c(master)/4d5e6f(feature-1)`；无提交（纯数据运维等）填 `无`。增量去重脚本按这列判重，漏填会导致下次误判重复记录。

**老表头兼容**：当天 md 若还是 9 列老表头（无提交id 列），追加行前先把表头行和分隔行末尾各补一列（` 提交id |`），存量数据行不动。

**需求概述写法**：🔴 **第一行先写一句无编号的标题**（概括这条需求），再从第二行起用 **dash 多级编号**列细节——日报管家会把标题拼成「一、标题」，编号子项各自成行，禁止概述以 `1、` 开头（会留下空的「一、」）。**所有编号统一带顿号**（`1、` / `1-1、` / `1-1-1、`）。示例：

```
归档三页 React 表单问题修复        ← 标题句(不带编号)
1、提交判定改全链路：不只凭 statusCode=200
1-1、启动流程失败也 200 → 走失败弹层不关窗
1-1-1、弹层文案 br 归一换行
2、ABS 存续清算归档回填修正
2-1、公告披露日回填与回显核对
（2次提交 09-05 13:42~13:52 · 分支 master · GitLab：https://gitlab.xx.com/g/lanxum-amisp-react · commit 1a2b3c/4d5e6f）
```

🔴 **末尾溯源括号必带四件套**：`（N次提交 MM-DD HH:MM~HH:MM · 分支 <branch> · GitLab：<仓库链接> · commit <短id>/<短id>）`。仓库链接、分支、commit id、提交日期取自第 1 步（日期取 `%ai`，与表格日期列一致用 MM-DD），链接已规整为网页地址；跨分支时 commit 标分支 `1a2b3c(master)/4d5e6f(feature-1)`。整条放概述最后一行。

编号规则：
- 首行标题无编号；一级要点 `1、2、`；每下钻一级在父号追加 `-N、`：`1-1、` → `1-1-1、` → `1-1-1-2、`
- **每个编号各自独立成行**，行间用 `<br>` 换行（同一行禁止塞多个编号）
- 底层细节（具体文件、行数）放最深层号或备注列，别让一级要点过长
- 每条末尾附涉及模块、N次提交与时间窗、+XXX/-XXX 行、GitLab 溯源括号（仓库链接/分支/commit id/日期）
- 🔴 **备注列必带该行耗时窗口** `⏱ MM-DD HH:MM~HH:MM（约 Xh）`（取该行**实际工作时段**，🔴 不取 commit 窗口——单 commit 日窗口会塌成 0，按实际人机配合时段写）；**全天压缩比**（＝分型常数 K，见「AI 辅助工时」节）放回复审计块，不重复进表

**🔴 markdown 表格安全规则**（不遵守必乱，三条血泪坑）：
1. **行间禁止空行** — 表格中间一个空行就把表格截断成两段，后半段变无表头散行（症状："第 N 行开始乱了"）。同一天多仓库、多批次记录也要连成一张表，行间不留空行；空行只能出现在整表结束之后。
2. **单元格禁止裸竖线 `|`** — 内容里出现 `||`（如 `isPdfType||isWordType`）会被解析成空列导致串行。用 `/` 分隔（`isPdfType/isWordType`）。
3. **单元格禁止裸尖括号 `<`/`>`** — `<tr>` 会被当 HTML 标签、`>` 可能触发引用，把表撑乱。改用文字（"表格行"）或 `→`（如「第三章→第一节」）。**唯一允许的尖括号是 `<br>`**（单元格内换行，必须用它）。

---

## 🔴 重记 / 重跑（用户说「删掉重新记录」「重做这天」时走这里）

用户**明说要重记**（而不是增量补记）时，**不走第 1.5 步去重**，按下面机械执行：

1. **对账**：先把当天 md 里被重记范围（该仓库的行，或用户没限定时整文件）的**行 + 末尾对应审计注释块**整段贴进回复，便于回滚对账。
2. **删旧**：删掉这些行与其对应 `<!-- 审计 … -->` 注释块；保留其他仓库的行、表头、分隔行、模板引言不动。整文件重记才清到模板（表头 + 分隔行）。
3. **重排序号**：剩余数据行序号按 1..N 重排，审计块 `seq=` 同步改（保证「审计 seq ↔ 表格 seq」对齐）。
4. **全量重跑**：对该仓库当天**全部** commit 重跑第 2 步合并 + 第 4 步评估，**不留旧数字**，重新机械求和。
5. **自检**：写完后复跑 `recorded_commits.py`，`unrecorded` 必须为空。
6. **回复**：输出写后审计块（同第 5 步）；编码仍按第 6 步（UTF-8 无 BOM、表格行间不留空行）。

---

## 自我校准 🔴 每次触发先回看，落盘必须确认（v1.2）

评估不是一次性的：每次触发本 skill，先跑一遍校准回看，让计价表、链路卡和压缩比 K 随实证进化，但**落盘必须架构师确认**，AI 不自改自价：

1. **回看**：读当天 md（已被管家合并则读 `已合并\` 当天分片）的**全部**审计注释块，按「仓库 + seq」定位对应表格行，机械对比注释里的公式字段与行内实值——v1.5 及以前为 `人工公式/AI公式`、v1.6/v1.7 开发类与文档类为 `人工公式/AI实测`、v1.6/v1.7 运维/实施类为 `人工=实测/AI=实测`，三类块都要能比对（运维类的「公式」即实测值本身，比对的是与行内数字是否一致）——不一致 = 偏差信号，标注「校准偏差：公式 X → 实值 Y（仓库 / 子项）」；无审计注释块的历史行（v1.1 时代）不参与比对
2. **累计**：同一链路卡 / 同一子项 **≥3 次同向偏差** 才够格触发进化提案；零星偏差只记录不折腾
3. **提案制**：提案只进回复（改哪张卡哪个价 / 哪条触点，依据哪几次偏差）；**架构师回确认词才写入 chains.md 或调单价**，调价升版本号记日期，双向可调（高了降、低了升），禁止静默改价、禁止只涨不跌
4. **新仓库转正**：没有对应链路卡的仓库首次记录后，按本次实证把「通用兜底卡」具体化为正式链路卡，同样走提案确认
5. **踩坑沉淀**：评估/验证中发现的新坑（方言、构建链、框架限制）提案追加进对应链路卡「已知坑」
6. **K 档校准（v2.0）**：压缩比 K 本身也受校准——回看时机械比对审计块 `AI=人工÷K(<K>)=<X.Xh>` 与行内 AI 实值，**人工改过数 = 该档 K 偏差信号**（辅信号：证据行「轮数/人工h」密度同档明显偏离，仅记录、不单独触发）；同一档 **≥3 次同向信号**才够格提案调 K，调 K 升版本号记日期、双向可调、仅对新记录生效、历史不复算；**样本不足不动 K**（宁缺勿滥）

---

## 严禁（红线速查 · 详细解释见对应小节）

**采集与去重（第 1 / 1.5 步）**
- 🔴 禁止混入他人提交、禁止 `git log` 加 `-n` 截断——只用 `today_commits.py` 或其 numstat 手工兜底
- 🔴 禁止用 `git diff --stat A..B` 统计变更量——`--author` 被静默忽略，同事提交混进总量（第 1 步）
- 🔴 禁止不跑 `recorded_commits.py` 肉眼判重、禁止合并展示漏掉「已跳过」清单、禁止写入后不复跑脚本自检（第 1.5 步防漏两条）
- 🔴 禁止提交id 列（第 10 列）空着——有提交写全部短 id，无提交写`无`；**无提交但有运维/实施工作（装库、部署、配置对齐、启停、发版）照样记**（按「运维/实施类记录口径」两列记实测），只有当天确实没干活才收工，禁止编造记录

**合并与评估（第 2 / 4 步）**
- 🔴 禁止不合并直接评估、禁止评估前不查链路卡、禁止跳过分型直接给 AI 工时
- 🔴 禁止区间值估算、禁止跳过维度直接报总数、禁止拍脑袋 2 秒给个数——只查计价表固定单价，AI 只数数量不选值
- 🔴 禁止把差异修改当机械替换、禁止同文件反复改每次算全价、禁止忽略异步/定时器等非直接交互的测试成本
- 🔴 禁止已完成任务用乘法返工公式（已完成的只用边界遗漏）、禁止逐任务累加沟通协作（按天只记一笔）、禁止把构建产物/纯机械迁移计入项数（只进备注）
- 🔴 禁止 D 类重构/迁移「新增代码项」超过迁移源单元数——多余拆分/重组单元并入已计迁移项、按 `min(实际项数, 源单元数)` 计（v2.0 封顶）；外部行为变化的部分按 C 类另计，不许借封顶漏算真实新增
- 🔴 禁止把 AI 辅助工时按「时间窗口 / 会话时长 / 对话轮数」估（v1.6/v1.7 口径已废）——v1.9 定为**人工工时 ÷ 压缩比 K（轴 2 档）**；禁止拿会话数/对话轮数反推、禁止按「人看不过来 N 个文件」做物理可行性二次修正；也🈲退回 v1.4/v1.5 的「单价 × 数量」等效投入式
- 🔴 禁止把**安装软件/部署/配置对齐/启停/发版**等运维/实施类（无 git 改动）套进五维单价表与代码数量口径——分型写「运维」、两列记实测、`⏱` 用实测起止；禁止借用其他行的提交时间窗（2026-09-10 序号 5 的教训）
- 🔴 禁止把 **`docs/` / `memory/` / `memory/ai/` 下的文档**当成「没提交就不记」，或混进开发类按代码项数算——文档类走「文档类单价表」（新建 0.5h/篇 + 正文 0.3h/百行 + 校验 5min/篇 + 修改 8min/处 + 边界 0.2h）；`memory/ai/` 不入 git 时按文件实际行数数

**写入与校准（第 5 / 6 步 / 自我校准）**
- 🔴 禁止中途等用户确认——合并、评估、写入全程零确认，算式全进「写后审计块」供事后核对
- 🔴 禁止重记时复用旧数字、留旧审计块——重记必须对当天全部 commit 重新机械求和，旧行与旧注释块一并删除（见「重记 / 重跑」）
- 🔴 禁止备注列漏写该行耗时窗口（`⏱ MM-DD HH:MM~HH:MM（约 Xh）`）、禁止为凑 8h 上限拆分日报记录——人工工时是等效产出（v1.1 起），超 8h 按 0.5h 舍入如实记录
- 🔴 禁止漏写 GitLab 溯源括号——概述末行必含「网页链接 + 分支 + commit id + 提交日期」，不许只写"N次提交"收尾
- 🔴 禁止表格行间留空行、单元格用裸 `|`/`<`/`>`——空行截断表格、`||` 拆空列、`<tr>`/`>` 被当 HTML（第 6 步表格安全规则）
- 🔴 禁止自我校准落盘不经确认——AI 只收集偏差信号和提案，改 chains.md / 调单价 / 调 K 必须架构师确认词
- 🔴 禁止无 ≥3 次同向实证就调 K——K 档校准同走提案制，样本不足不动 K，调 K 升版本号、仅对新记录生效、历史不复算（v2.0）

---

> 工时评估不是拍脑袋——先合再算、**先判类型（开发 / 文档 / 运维）**、分型查卡、单价定死、已完成不乘、沟通共享、等效产出不设限、**AI 辅助工时记实际投入**、不拿窗口凑数；全程零确认，算式落盘可校准、偏差三次必提案。少算一个维度就是对自己的不尊重。
