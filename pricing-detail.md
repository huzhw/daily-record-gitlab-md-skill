# 计价细则与例算（pricing-detail）

> 单价与判据的**当前生效版**在 SKILL.md「工时评估规则」（计价表 v2.3）。本文件是配套细则、例算与实证存档，按需读——**两者冲突时以 SKILL.md 为准**。

---

## 簇切分 ×0.6 实例（2026-09-10 workingpaper-v5.5）

- 簇 = 采集链路后端（≈719 行，锚，不打折）/ React 查询页（≈547）/ 统计页（≈390）/ Quartz 注释 sweep（1290 行，依附 a）/ 菜单更名（1 行，依附 b）→ 后四者 ×0.6
- 注：后端与查询页同出 `139384582` 一个 commit，按「同一 commit 多端交付照拆」判据仍分两簇

## 文档类例算（校准用）

- 新建 200 行文档 = 0.5（新建）+ 0.6（正文 2×0.3）+ 0.08（校验）+ 0.2（边界）= 1.38 → **1.5h**
- `MEMORY.md` 加一行索引 = 3min → **0.25h**（单条最低档）
- 改 5 处结论 = 5×8min + 校验 0.08 + 边界 0.2 = 0.95 → **1h**

## 设计文档 K 档实证（v2.3 依据）

- 2026-09-16 JVM 监控设计文档：实际人机配合约 2h；旧口径（正文价 + 档③）仅折 AI 0.5h，偏差 4 倍 → 架构师拍板升版，设计文档不再默认档③，按参照情况定档⑤/档⑥

## 测试双份制由来（v2.2）

- 架构师 2026-09-15 指示：「你测一次我测一次，AI 的打折、本人测的不打折，双份都要记」
- 教训：2026-09-15 归档统计任务把 3h 本人手测按 K 折成 1.2h，被认定为篡改实测——由此立红线「禁止把本人手测时间按 K 折算」

## 运维/实施类立卡教训（v1.6 依据）

- 2026-09-10 序号 5（139 装 OceanBase + Redis）：无 commit 的装库工作被代码单价表硬算成 4.12h，`⏱` 还借用了另一个仓库行的提交时间（编造）→ 立卡 7 实测制：两列记实测、`⏱` 必须实测起止、禁止套代码单价

## 文档类立卡教训（v1.7 依据）

- 2026-09-10 日报中 `memory/` 下的文档（CVE 漏洞澄清清单、jar 依赖清单、服务器清单、索引基线等）被并进开发类按代码项数算 → v1.7 单列文档类，走文档单价表

## 历史口径废存对照

| 口径 | 状态 |
|------|------|
| v1.4/v1.5「AI 单价求和式」（设计协助 / 代码生成 / 审查验证 / 修正对话四项单价） | v1.9 起整体废除，仅存档于 git 历史，禁止回用 |
| v1.6/v1.7「AI辅助＝人机配合实测墙钟」（按时间窗口记） | v1.9 起废除；时间窗只进备注 `⏱` 与审计证据行 |
| 按会话数 / 对话轮数 / 「人看不过来 N 个文件」物理可行性修正 AI 工时 | 禁止（v1.9 红线） |
| 审计块历史字段名（v1.5 前 `人工公式/AI公式`、v1.6/v1.7 `人工公式/AI实测`、`人工=实测/AI=实测`） | 历史块照认（`calibrate.py` 兼容解析）；新块统一 `审计格式 v1`（详见 CHANGELOG「审计块格式版本」） |
